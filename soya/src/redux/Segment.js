/**
 * Segment is really just a static object with actionCreator and reducer as its
 * properties. It represents a segment of our store, a top level key in our
 * state tree.
 *
 * @CLIENT_SERVER
 */
export default class Segment {
  static get actionCreator() {

  }

  static get reducer() {

  }
}